@model myfinance_web_netcore.Models.PlanoContas

@{
    ViewData["Title"] = "Novo Plano de Contas";
}

<div class="container">
    <div class="row">
        <div class="col-12">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item">
                        <a asp-action="Index">Plano de Contas</a>
                    </li>
                    <li class="breadcrumb-item active" aria-current="page">Novo</li>
                </ol>
            </nav>

            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2><i class="fas fa-plus-circle"></i> Novo Plano de Contas</h2>
                <a asp-action="Index" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Voltar
                </a>
            </div>

            <div class="card">
                <div class="card-body">
                    <form asp-action="Create" method="post">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
                        
                        <div class="row">
                            <div class="col-md-3">
                                <div class="form-group mb-3">
                                    <label asp-for="Codigo" class="form-label"></label>
                                    <input asp-for="Codigo" class="form-control" placeholder="Ex: 1" />
                                    <span asp-validation-for="Codigo" class="text-danger"></span>
                                    <small class="form-text text-muted">Código único para identificação</small>
                                </div>
                            </div>
                            
                            <div class="col-md-6">
                                <div class="form-group mb-3">
                                    <label asp-for="Descricao" class="form-label"></label>
                                    <input asp-for="Descricao" class="form-control" placeholder="Ex: Combustível" />
                                    <span asp-validation-for="Descricao" class="text-danger"></span>
                                    <small class="form-text text-muted">Descrição do plano de contas</small>
                                </div>
                            </div>
                            
                            <div class="col-md-3">
                                <div class="form-group mb-3">
                                    <label asp-for="Tipo" class="form-label"></label>
                                    <select asp-for="Tipo" class="form-select">
                                        <option value="">Selecione o tipo</option>
                                        <option value="0">💸 Despesa</option>
                                        <option value="1">💰 Receita</option>
                                    </select>
                                    <span asp-validation-for="Tipo" class="text-danger"></span>
                                </div>
                            </div>
                        </div>

                        <div class="row mt-4">
                            <div class="col-12">
                                <div class="d-flex gap-2">
                                    <button type="submit" class="btn btn-success">
                                        <i class="fas fa-save"></i> Salvar
                                    </button>
                                    <a asp-action="Index" class="btn btn-secondary">
                                        <i class="fas fa-times"></i> Cancelar
                                    </a>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Card de Ajuda -->
            <div class="card mt-4">
                <div class="card-header">
                    <h6 class="mb-0"><i class="fas fa-info-circle"></i> Dicas</h6>
                </div>
                <div class="card-body">
                    <ul class="mb-0">
                        <li><strong>Código:</strong> Use números sequenciais (1, 2, 3...) para facilitar a organização</li>
                        <li><strong>Descrição:</strong> Use nomes claros e objetivos (ex: "Supermercado", "Salário")</li>
                        <li><strong>Tipo:</strong> Receita para dinheiro que entra, Despesa para dinheiro que sai</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
}
